<%= form_for(@selection_session) do |f| %>
  <% if @selection_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@selection_session.errors.count, "error") %> prohibited this selection_session from being saved:</h2>

      <ul>
      <% @selection_session.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
